CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

INCLUDE_DIRECTORIES(BEFORE ${CMAKE_CURRENT_SOURCE_DIR}/../sigar/include)

ADD_EXECUTABLE(sigar_port sigar_port.c)
TARGET_LINK_LIBRARIES(sigar_port sigar)

INSTALL(TARGETS sigar_port
        RUNTIME DESTINATION bin)
